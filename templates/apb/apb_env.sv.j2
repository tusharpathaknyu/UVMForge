//------------------------------------------------------------------------------
// APB Environment - Generated by UVMForge
//------------------------------------------------------------------------------

class {{ prefix }}_env extends uvm_env;
    
    `uvm_component_utils({{ prefix }}_env)
    
    // Components
    {{ prefix }}_agent      agent;
{% if has_scoreboard %}
    {{ prefix }}_scoreboard scoreboard;
{% endif %}
{% if has_coverage %}
    {{ prefix }}_coverage   coverage;
{% endif %}
    
    // Constructor
    function new(string name = "{{ prefix }}_env", uvm_component parent = null);
        super.new(name, parent);
    endfunction
    
    // Build phase
    virtual function void build_phase(uvm_phase phase);
        super.build_phase(phase);
        
        agent = {{ prefix }}_agent::type_id::create("agent", this);
{% if has_scoreboard %}
        scoreboard = {{ prefix }}_scoreboard::type_id::create("scoreboard", this);
{% endif %}
{% if has_coverage %}
        coverage = {{ prefix }}_coverage::type_id::create("coverage", this);
{% endif %}
    endfunction
    
    // Connect phase
    virtual function void connect_phase(uvm_phase phase);
        super.connect_phase(phase);
        
{% if has_scoreboard %}
        // Connect agent monitor to scoreboard
        agent.ap.connect(scoreboard.ap);
{% endif %}
{% if has_coverage %}
        // Connect agent monitor to coverage
        agent.ap.connect(coverage.analysis_export);
{% endif %}
    endfunction
    
endclass : {{ prefix }}_env
